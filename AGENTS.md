# Dockline coding conventions

Use CODING_STYLE.md. Keep four-space indentation, single quotes, compact imports and one blank line between logical steps. Keep the generic SDK independent of configuration importers and applications. Run npm run lint, npm run build and the relevant tests. Generated output and dependencies are excluded from formatting.
